components:
  schemas:
    ServerError:
      type: object
      properties:
        status:
          type: string
          example: error
          description: estado de la respuesta
        message:
          type: string
          example: Internal server error
          description: mensaje de error